pandas>=2.0,<3
numpy>=1.24,<3
# Model artifacts in ml/artifacts were serialized with sklearn 1.6.1.
# Keep this pinned unless you retrain and replace all joblib artifacts.
scikit-learn==1.6.1
joblib>=1.3,<2
